import { Metadata } from "next"

export const metadata: Metadata = {
  title: "Get Involved",
  description: "Partner with us, volunteer, sponsor a student, or donate to make a lasting difference in the community.",
}

export default function GetInvolvedLayout({ children }: { children: React.ReactNode }) {
  return children
}
