import { Metadata } from "next"

export const metadata: Metadata = {
  title: "News & Impact",
  description: "Read stories of change and stay updated on the latest outreaches and events from Arise CSF.",
}

export default function NewsImpactLayout({ children }: { children: React.ReactNode }) {
  return children
}
