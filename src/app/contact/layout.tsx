import { Metadata } from "next"

export const metadata: Metadata = {
  title: "Contact Us",
  description: "Get in touch with Arise CSF in Benin City. We are always looking for partners, volunteers, and caring hearts.",
}

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children
}
